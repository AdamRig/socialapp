<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>
<p>
	<% @category.posts.each do |post| %>
		<br>
		<b><%= post.user.name %></b>
		&emsp; <%= post.content %> &emsp;
	<% end %>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
